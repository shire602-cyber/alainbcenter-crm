/**
 * Marketing Layout
 * Layout for marketing pages (public, no auth required)
 */

import React from 'react'

export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}












